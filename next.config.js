/** @type {import('next').NextConfig} */
const nextConfig = {
  
}

module.exports = nextConfig

module.exports = {
    images: {
      domains: ['www.diariomotor.com', 'logos-world.net', "hips.hearstapps.com"], 
    },
  };